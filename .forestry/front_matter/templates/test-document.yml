---
label: Test Document
hide_body: true
display_field: name
fields:
- name: name
  type: text
  config:
    required: false
  label: Naam
- name: blocks
  type: blocks
  label: blocks
  template_types:
  - author
  config:
    min: 
    max: 
- name: schrijvers
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: test
  label: schrijvers
pages:
- content/test/Bert.md
